include ../partials/header.pug
h1 Qui prend quoi ?
h2= party.name
h3 Créé par #{party.author}
label(for="url") Partagez l'événement
input(id="url" value=`${url}` data-clipboard=`${url}` data-share-url=`${url}` data-share-title=`${party.name}` data-share-text=`${party.author} vous invite à participer à l'évenement ` )

form(method="post" action="/party/${id}/")
  div
    label(for="item") Rajout d'un item à l'évenement
    input(name="item" id="item" required minlength="3" placeholder="objet")

  button(type="submit") Créer

script.
  party = !{JSON.stringify(party)}